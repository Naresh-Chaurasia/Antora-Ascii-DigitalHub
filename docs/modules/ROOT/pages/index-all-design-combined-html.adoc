++++
<style>
.dashboard-header {
  text-align: center;
  margin-bottom: 40px;
  padding: 30px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 15px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.1);
}
.dashboard-header h1 {
  margin: 0;
  font-size: 2.5em;
  font-weight: 700;
}

/* Div-based grid layout */
.dashboard-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
  width: 100%;
}

.bucket {
  background: #fafafa;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  padding: 20px;
  min-height: 200px;
}

/* Color coding */
.bucket:nth-child(1) {
  border-left: 4px solid #2196F3;
}

.bucket:nth-child(2) {
  border-left: 4px solid #4CAF50;
}

.bucket:nth-child(3) {
  border-left: 4px solid #FF9800;
}

.bucket:nth-child(4) {
  border-left: 4px solid #9C27B0;
}

/* Headers */
.bucket strong {
  display: block;
  font-size: 1.1em;
  font-weight: 600;
  margin-bottom: 15px;
  color: #333;
}

.bucket strong a {
  color: #333;
  text-decoration: none;
  transition: color 0.2s ease;
}

.bucket strong a:hover {
  color: #667eea;
  text-decoration: underline;
}

/* Simple TOC styling */
.toc-simple {
  background: #f8f9fa;
  border: 1px solid #e9ecef;
  border-radius: 8px;
  padding: 20px;
  margin-bottom: 30px;
}

.toc-simple h3 {
  margin: 0 0 15px 0;
  color: #333;
  font-size: 1.2em;
  text-align: center;
}

.toc-simple ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.toc-simple li {
  margin-bottom: 8px;
}

.toc-simple li a {
  color: #495057;
  text-decoration: none;
  font-weight: 500;
  font-size: 0.95em;
  display: block;
  padding: 8px 12px;
  border-radius: 6px;
  transition: all 0.2s ease;
}

.toc-simple li a:hover {
  color: #667eea;
  background: #f0f7ff;
  text-decoration: none;
}

/* Section styling */
.section {
  margin-bottom: 20px;
}

.section:last-child {
  margin-bottom: 0;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
  padding: 8px 0;
  border-bottom: 1px solid #e5e7eb;
  transition: all 0.2s ease;
}

.section-header:hover {
  border-bottom-color: #d1d5db;
}

.section-header strong {
  font-size: 0.9em;
  font-weight: 600;
  color: #666;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 0;
}

.toggle-icon {
  font-size: 0.8em;
  color: #9ca3af;
  transition: transform 0.2s ease;
}

.section-header.collapsed .toggle-icon {
  transform: rotate(-90deg);
}

.section-content {
  padding-top: 10px;
  overflow: hidden;
  transition: all 0.3s ease;
}

.section-content.collapsed {
  max-height: 0;
  padding-top: 0;
  opacity: 0;
}

/* Task lists */
.bucket ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.bucket li {
  background: white;
  padding: 8px 12px;
  margin-bottom: 6px;
  border-radius: 4px;
  border: 1px solid #e8e8e8;
  font-size: 0.9em;
  transition: all 0.2s ease;
  cursor: pointer;
}

/* Different border colors for each task item */
.bucket li:nth-child(1) { border-color: #4A90E2; }
.bucket li:nth-child(2) { border-color: #7ED321; }
.bucket li:nth-child(3) { border-color: #F5A623; }
.bucket li:nth-child(4) { border-color: #BD10E0; }
.bucket li:nth-child(5) { border-color: #50E3C2; }
.bucket li:nth-child(6) { border-color: #B8E986; }
.bucket li:nth-child(7) { border-color: #F8B500; }
.bucket li:nth-child(8) { border-color: #9013FE; }

.bucket li:hover {
  background: #f0f7ff;
  transform: translateX(2px);
}

.bucket li::before {
  content: none;
}

/* Comment styling */
.bucket .comment {
  margin-left: 20px;
  margin-top: 6px;
  margin-bottom: 12px;
  padding: 8px 12px;
  background: #f8f9fa;
  border-left: 3px solid #6c757d;
  border-radius: 4px;
  font-size: 0.85em;
  color: #495057;
  text-align: left !important;
  display: block;
  width: 100%;
  direction: ltr;
}

.bucket .comment ul {
  list-style: none;
  padding: 0;
  margin: 0;
  text-align: left !important;
  direction: ltr;
}

.bucket .comment * {
  text-align: left !important;
  direction: ltr;
}

.bucket .comment li {
  background: transparent;
  padding: 2px 0;
  margin-bottom: 4px;
  border: none;
  font-size: 0.9em;
  text-align: left !important;
  direction: ltr;
}

.bucket .comment li:last-child {
  margin-bottom: 0;
}

.bucket .comment li::before {
  content: "•";
  color: #6c757d;
  margin-right: 8px;
  font-weight: bold;
}

/* Priority labels */
.bucket li em {
  font-style: normal;
  font-size: 0.8em;
  font-weight: 600;
  margin-left: 8px;
  padding: 2px 8px;
  border-radius: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* Priority colors */
.bucket li .priority-high {
  background: #fee2e2;
  color: #dc2626;
  border: 1px solid #fecaca;
}

.bucket li .priority-medium {
  background: #fef3c7;
  color: #d97706;
  border: 1px solid #fde68a;
}

.bucket li .priority-low {
  background: #d1fae5;
  color: #059669;
  border: 1px solid #a7f3d0;
}

/* Status indicators */
.bucket li .status {
  font-style: normal;
  font-size: 0.8em;
  font-weight: 600;
  margin-left: 4px;
  padding: 2px 6px;
  border-radius: 10px;
  text-transform: uppercase;
  letter-spacing: 0.3px;
}

.bucket li .status-done {
  background: #dcfce7;
  color: #166534;
  border: 1px solid #bbf7d0;
}

.bucket li .status-in-progress {
  background: #dbeafe;
  color: #1e40af;
  border: 1px solid #bfdbfe;
}

.bucket li .status-pending {
  background: #f3f4f6;
  color: #6b7280;
  border: 1px solid #e5e7eb;
}

.bucket li .status-blocked {
  background: #fef2f2;
  color: #991b1b;
  border: 1px solid #fecaca;
}

/* Bucket separators */
.bucket-separator {
  text-align: center;
  margin: 30px 0;
  padding: 20px 0;
}

.bucket-separator hr {
  border: none;
  height: 1px;
  background: linear-gradient(to right, transparent, #d1d5db, transparent);
  margin: 10px 0;
}

.separator-text {
  color: #6b7280;
  font-size: 0.9em;
  font-style: italic;
  margin: 0;
  padding: 0 20px;
  background: #fafafa;
  display: inline-block;
}


/* Mobile responsive */
@media (max-width: 768px) {
  .dashboard-grid {
    grid-template-columns: 1fr;
    gap: 15px;
  }
  
  .bucket-separator {
    margin: 20px 0;
    padding: 15px 0;
  }
  
  .separator-text {
    font-size: 0.8em;
  }
}
</style>

<script>
function toggleSection(header) {
  const content = header.nextElementSibling;
  const isCollapsed = header.classList.contains('collapsed');
  
  if (isCollapsed) {
    header.classList.remove('collapsed');
    content.classList.remove('collapsed');
  } else {
    header.classList.add('collapsed');
    content.classList.add('collapsed');
  }
}
</script>
++++